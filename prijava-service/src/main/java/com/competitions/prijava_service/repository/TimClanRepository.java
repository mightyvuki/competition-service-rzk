package com.competitions.prijava_service.repository;

import com.competitions.prijava_service.model.TimClan;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimClanRepository extends JpaRepository<TimClan, Long> {
}